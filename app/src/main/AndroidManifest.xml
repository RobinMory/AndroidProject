<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.helha.ue3103.android_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android_Project">
        <activity
            android:name=".controlers.ProjectActivity"
            android:exported="true" />
        <activity
            android:name=".controlers.AddStudentActivity"
            android:exported="true" />
        <activity
            android:name=".controlers.StepListActivity"
            android:exported="true" />
        <activity
            android:name=".controlers.StudentListActivity"
            android:exported="true"
            android:label="MPM">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>